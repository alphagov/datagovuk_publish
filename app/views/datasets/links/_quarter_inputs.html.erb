<h2 class="form-label-bold">Time period for this link</h2>
<div class="form-group <% if @link.errors.key? "quarter" %>form-group-error<%end%>">
  <fieldset>
    <legend>Quarter</legend>

    <% if @link.errors.key? "quarter" %>
      <span class="error-message">
        Please select a quarter
      </span>
    <% end %>

    <div class="multiple-choice">
      <%= radio_button_tag 'link[quarter]', '1', @link.quarter == 1, id: 'q1' %>
      <label for="q1">Q1 (April to June)</label>
    </div>

    <div class="multiple-choice">
      <%= radio_button_tag 'link[quarter]', '2', @link.quarter == 2, id: 'q2' %>
      <label for="q2">Q2 (July to September)</label>
    </div>

    <div class="multiple-choice">
      <%= radio_button_tag 'link[quarter]', '3', @link.quarter == 3, id: 'q3' %>
      <label for="q3">Q3 (October to December)</label>
    </div>

    <div class="multiple-choice">
      <%= radio_button_tag 'link[quarter]', '4', @link.quarter == 4, id: 'q4' %>
      <label for="q4">Q4 (January to March)</label>
    </div>
  </fieldset>
</div>

<div class="form-group<% if @link.errors[:start_year].present? %> form-group-error<% end %>">
  <fieldset>
    <legend class="visuallyhidden">Year</legend>
    <div class="form-date">
      <div id="year_form_group" class="form-group form-group-year ">
        <label class="form-label-bold" for="period_year">Year</label>
        <% if @link.errors[:start_year].present? %>
          <span class="error-message">Please enter a valid year</span>
        <% end %>
        <%= text_field_tag 'link[start_year]', @link.start_date.year, id: "period_year", class: "form-control" %>
      </div>
    </div>
  </fieldset>
</div>

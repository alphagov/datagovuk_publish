default: &default
  host: http://localhost:9200

test:
  <<: *default

development:
  <<: *default

staging: 
  host: <%= ENV['ES_HOST'] %>

production: &default
  vcap_services: <%= ENV['VCAP_SERVICES'] %>

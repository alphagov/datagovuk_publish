---
:concurrency: <%= ENV.fetch('RAILS_MAX_THREADS') { 5 } %>
:queues:
  - sync
  - import
:limits:
  sync: 1
  import: <%= ENV.fetch('RAILS_MAX_THREADS') { 5 } - 1 %>

# sidekiq schedule is replaced by cronjobs the govuk-dgu-charts repo

---
:concurrency: <%= ENV.fetch('RAILS_MAX_THREADS') { 5 } %>

:queues:
  - default
  - importer
  - indexer
  - link_checker

:schedule:
  model_metrics:
    every: '1m'
    class: ModelMetricsWorker
